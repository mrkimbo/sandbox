"use strict";var elements={root:document.getElementById("main-container"),header:document.getElementById("header"),content:document.getElementById("content"),container:document.getElementById("page-container"),pages:Array.prototype.constructor.apply(null,document.querySelectorAll(".page"))};var answers=[0,0,0];var dirty=false;var maxScroll;var screenSize=new Rect();init();function init(){var a;elements.pages.forEach(function(b){a=Array.prototype.constructor.apply(null,b.querySelectorAll(".options li"));a.forEach(function(c){c.addEventListener("click",optionClickHandler,false)})});window.addEventListener("resize",onResize,false);onResize(null)}function onResize(a){redrawPages();redrawTweens()}function redrawPages(){elements.content.style.height=elements.root.clientHeight-78+"px";maxScroll=elements.content.scrollHeight-elements.content.clientHeight;screenSize.width=elements.content.clientWidth;screenSize.height=elements.content.clientHeight}loop();function loop(){if(dirty){redraw();checkDirty()}_raf(loop)}function redraw(){}function checkDirty(){dirty=false};